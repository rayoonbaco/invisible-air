# Observation Atlas case packages

A case may become selectable only after every field in `CASE_PACKAGE_TEMPLATE.json` is populated with traceable evidence. The Atlas must never substitute invented source geometry, wind, timestamps, or citations.
