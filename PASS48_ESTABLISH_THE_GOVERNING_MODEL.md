# Pass 48 — Establish the Governing Model

This package was built directly from Pass 47 — The Architecture of Understanding.

## Run the existing application
Double-click `RUN_OBSERVATORY.bat`.

## Verify Pass 48
Double-click `RUN_VERIFY_PASS48.bat` in this folder.

The verifier runs eight deterministic model tests and regenerates the three benchmark field packages in:

`validated_core\artifacts\pass48`

The Observatory visual is intentionally unchanged in Pass 48. Pass 49 will connect the new governing field to the visible atmospheric layer.
